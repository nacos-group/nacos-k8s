apiVersion: v1
kind: Service
metadata:
  name: {{ include "nacos-operator.fullname" . }}
  labels:
    {{- include "nacos-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nacos-operator.selectorLabels" . | nindent 4 }}
